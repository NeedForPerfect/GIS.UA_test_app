import { HoverActiveDirective } from './hover-active.directive';

describe('HoverActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new HoverActiveDirective();
    expect(directive).toBeTruthy();
  });
});
